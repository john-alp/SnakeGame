import org.w3c.dom.ls.LSOutput;

import java.util.Scanner;

/**
 * Write a program that reads an array of integers and finds the minimum value of the array.
 *
 * Input data format
 *
 * The first line contains the size of an array.
 * The second line contains elements of the array separated by spaces.
 *
 * Output data format
 *
 * An integer number representing the minimum in the input array.
 * Sample Input 1:
 *
 * 5
 * 5 1 4 2 3
 * Sample Output 1:
 *
 * 1
 */
/*
Write a program that reads an unsorted array of integers and two numbers n and m.
        The program must check if n and m occur next to each other in the array (in any order).

*/
public class Test {

  public static void main(String[] args) {


//      char[] chars = {'A', 'B', 'C', 'D', 'E', 'F'};
//      String stringFromChars = String.valueOf(chars); // "ABCDEF"
//      System.out.println(stringFromChars);
//
//      char[] charsFromString = stringFromChars.toCharArray(); // { 'A', 'B', 'C', 'D', 'E', 'F' }
//      String theSameString = new String(charsFromString); // "ABCDEF"
//      System.out.println(theSameString);
//      System.out.println("*");
//
//      String sentence = "a long text";
//      String[] words = sentence.split(" "); // {"a", "long", "text"}
//      for (String a : words) {
//          System.out.println(a);
//      }
//      String scientistName = "Isaac Newton";
//
//      for (int i = 0; i < scientistName.length(); i++) {
//          System.out.println(scientistName.charAt(i) + " "); // print the current character
//      }
//
//      char[] c2hars = {'H', 'Y', 'P', 'E', 'R', '-', 'S', 'K', 'I', 'L', 'L'};
//
//      String s2tringFromChars = String.valueOf(c2hars);
//      System.out.println(s2tringFromChars);
//
//      String[] strings = s2tringFromChars.split("-");
//      System.out.println(strings[1]);

      String str1 = "aaabbcccdaa";
      String str2 = " ";

//      for (int i = 0; i < str1.length(); i++) {
//          char ch = str1.charAt(i);
//          if (ch != str2.charAt(str2.length() - 1)) {
//              str2 += ch;
//          }
//
//      }
//      System.out.println(str2);
  }


//     Scanner scanner = new Scanner(System.in);
//  int sizeOfArr = scanner.nextInt(); //размер создаваемого массива
//  int currentMinimum = Integer.MAX_VALUE; //заполняем массив
//  while (sizeOfArr > 0) {
//   int currentVal = scanner.nextInt();
//   if (currentVal < currentMinimum) {
//    currentMinimum = currentVal;
//   }
//   sizeOfArr--;
//  }
//  System.out.println(currentMinimum);
 }


